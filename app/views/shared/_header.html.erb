<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand ml-3" href="/">beginmate</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse d-flex justify-content-end" id="navbarNav">
      <ul class="navbar-nav d-flex align-items-center">
        <li class="nav-item">
          <a class="nav-link" href="#">메이트 찾기</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">메이트 모집</a>
        </li>

        <% if user_signed_in? %>
          <div class="dropdown">
            <a class="btn dropdown" role="button" id="dropdownMenuLink" data-bs-toggle="dropdown" aria-expanded="false">
              <% if current_user.profile.attached? %>
                <img src="<%= url_for(current_user.profile) %>" alt="" class="logo_img rounded-circle">
              <% else %>
                <img src="https://picsum.photos/seed/picsum/200/300" alt="" class="logo_img rounded-circle">
              <% end %>
            </a>
            <ul class="dropdown-menu" aria-labelledby="dropdownMenuLink">
              <li><a class="dropdown-item" href="#"><%= current_user.email %></a></li>
              <li><a class="dropdown-item" href="/teams/new">새 팀 생성</a></li>
              <% current_user.teams.each do |team| %>
                <li><a class="dropdown-item" href="/teams/<%= team.id %>"><%= team.name %></a></li>
              <% end %>
              <li><a class="dropdown-item" href="/users/edit" >개인 정보 수정</a></li>
              <li><a class="dropdown-item" href="/users/sign_out" data-method="delete">로그아웃</a></li>
            </ul>
          </div>

        <% else %>
          <a class="nav-link" href="/users/sign_up">로그인/회원가입</a>
        <% end %>
      </ul>
    </div>
  </div>
</nav>